type TabKeys = "viewall" | "savemoney" | "tips" | "guides";

type LoadingState = {
  text: string;
};

type AutoYear = {
  id: number;
  year: string;
};

type VechileData = {
  id: number;
  make: string;
  model: string;
};